name: 'Update flakes'

on:
  workflow_dispatch:
  schedule:
    - cron: '51 2 * * 0'

jobs:
  createPullRequest:
    uses: loophp/flake-lock-update-workflow/.github/workflows/upgrade-flakes.yaml@main
