name: EditorConfig checks

on:
  pull_request:

jobs:
  lockfile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check files for EditorConfig compliance
        uses: editorconfig-checker/action-editorconfig-checker@7da587d0b76619c9cee373305cdd05c57e9e2950 # untagged main
        with:
          version: 2.7.0
